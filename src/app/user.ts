export class user {
    id: number;
    nickName: string;
    email: string;
    password: string;

    constructor(
        email: string,
        username: string,
        password: string
    ) {}
}